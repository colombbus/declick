{
  "name": "root",
  "version": "1.0.0",
  "license": "GPL-3.0",
  "private": true,
  "description": "",
  "scripts": {
    "unit": "lerna run unit --scope={@declick/runtime,@declick/objects}",
    "unit:watch": "lerna run --parallel unit:watch --scope={@declick/runtime,@declick/objects}",
    "serve": "lerna run --parallel start",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "workspaces": [
    "packages/client",
    "packages/objects",
    "packages/runtime"
  ]
}
